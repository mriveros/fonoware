<% if @actualizado %>
  
  $("#form_buscar_citas_id").val('<%= @cita.id %>');
  $("#form-buscar-citas").submit();

   setTimeout(function(){ 

    swal({title: "Atención", text: "<%= j(@msg)%>", html: false})
    
  }, 2000);

<% else %>

    swal({title: "Atención", text: "<%= j(@msg)%>", html: false})

<% end %>