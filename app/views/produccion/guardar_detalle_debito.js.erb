<% if @guardado_ok %>

	$('#<%=params[:window_id]%>').modal('hide');
	$('#<%=params[:window_id]%>').html('');

   swal({title: "Atención", text: "<%= j(@msg)%>", html: false})
   $("#link-to-produccion-detalle-debito-<%= params[:produccion_id]%>").trigger('click');
   $("#link-to-produccion-detalle-debito-<%= params[:produccion_id]%>").trigger('click');

<% else %>

  swal('No se pudo agregar del detalle de Débito');

<% end %>