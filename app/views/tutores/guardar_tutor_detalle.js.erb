<% if @guardado_ok %>

 
setTimeout( function(){ 
  
    $("#<%= params[:window_id]%>").modal('hide');
    $("#link-to-tutor-detalle-<%= params[:tutor_id]%>").trigger('click');
    $("#link-to-tutor-detalle-<%= params[:tutor_id]%>").trigger('click');
    swal("Los detalles de Tutores se guardaron exitosamente!");

    }, 1000);
 
<% else %>

  swal('Error al guardar los detalles de Tutores. Intente más tarde');

<% end %>